# Copiá este archivo a .env.local (nunca se commitea) y completá tu key real.
# Se usa únicamente server-side, en src/services/geminiServer.ts —
# nunca se expone al navegador.
# Conseguila gratis en https://aistudio.google.com
GEMINI_API_KEY=
